package com.ai.assistance.aiterminal.terminal.ai

class ThinkingProcessTracker

enum class StepType { DEFAULT }

data class ThinkingStep(val placeholder: String = "")

data class ThinkingSession(val placeholder: String = "")

enum class SessionStatus { DEFAULT }
