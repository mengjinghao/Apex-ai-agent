package com.apex.agent.kernel.burst.enhanced.progress

class ProgressTracker

data class Progress(val placeholder: String = "")

data class ProgressEvent(val placeholder: String = "")

enum class ProgressEventType { DEFAULT }
