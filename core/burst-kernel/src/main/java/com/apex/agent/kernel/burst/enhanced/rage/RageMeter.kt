package com.apex.agent.kernel.burst.enhanced.rage

class RageMeter

enum class BerserkState { DEFAULT }

data class RageEvent(val placeholder: String = "")

enum class RageEventType { DEFAULT }

data class RageBoost(val placeholder: String = "")

enum class FailureSeverity { DEFAULT }
