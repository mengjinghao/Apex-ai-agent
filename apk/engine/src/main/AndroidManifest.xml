<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:sharedUserId="com.apex.agent.suite"
    android:sharedUserMaxSdkVersion="28">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.WRITE_SECURE_SETTINGS" tools:ignore="ProtectedPermissions" />
    <uses-permission android:name="android.permission.DUMP" tools:ignore="ProtectedPermissions" />
    <uses-permission android:name="android.permission.BIND_ACCESSIBILITY_SERVICE" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <application
        android:name=".EngineApplication"
        android:allowBackup="false"
        android:label="@string/apk_label"
        android:process="${apexMainProcess}"
        tools:replace="android:label,android:theme"
        android:supportsRtl="true"
        android:theme="@style/Theme.ApexSuite"
        tools:targetApi="34">
        <service
            android:name=".EngineHostService"
            android:permission="com.apex.sdk.bridge.permission.BIND"
            android:exported="true">
            <intent-filter>
                <action android:name="com.apex.sdk.bridge.BIND_ENGINE" />
            </intent-filter>
        </service>
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.ApexSuite">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
