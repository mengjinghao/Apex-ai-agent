package com.apex.util

// Minimal implementation (original had 12 errors)
// TODO: Restore full implementation from original code

object NotificationUtils {
    fun init() { }
}
data class NotificationChannelConfig(val data: String = "")
