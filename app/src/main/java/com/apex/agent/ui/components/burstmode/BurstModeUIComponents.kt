package com.apex.agent.ui.components.burstmode

/**
 * 狂暴模式 UI 组件 — Stub 包占位。
 * 原包含 BurstModeCard / BurstModeProgressBar 等 Compose 组件，
 * 已随 UI 层移除。业务代码使用 wildcard import 引用此包，
 * 此文件保留包结构，避免编译错误。
 *
 * 如需恢复，添加 Compose 组件到此包下即可。
 */
object BurstModeUIComponents {
    const val TAG = "BurstModeUIComponents"
}
