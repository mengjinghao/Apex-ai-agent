package com.apex.util

// Minimal implementation (original had 10 errors)
// TODO: Restore full implementation from original code

object MediaPoolManager {
    fun init() { }
}
data class TranscodedMedia(val data: String = "")
class AsciiStringInputStream
data class MediaData(val data: String = "")
