package com.apex.util

// Minimal implementation (original had 85 errors)
// TODO: Restore full implementation from original code

object AppIconManager {
    fun init() { }
}
enum class AppIconType { DEFAULT }
fun switchIcon() { }
