package com.apex.agent.util

// Minimal implementation (original had 13 errors)
// TODO: Restore full implementation from original code

data class PerformanceMetrics(val data: String = "")
class PerformanceMonitor
data class MemoryInfo(val data: String = "")
enum class HealthStatus { DEFAULT }
object PerformanceUtils {
    fun init() { }
}
