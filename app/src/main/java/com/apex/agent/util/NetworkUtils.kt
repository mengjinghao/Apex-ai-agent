package com.apex.util

// Minimal implementation (original had 2 errors)
// TODO: Restore full implementation from original code

object NetworkUtils {
    fun init() { }
}
enum class NetworkSpeed { DEFAULT }
