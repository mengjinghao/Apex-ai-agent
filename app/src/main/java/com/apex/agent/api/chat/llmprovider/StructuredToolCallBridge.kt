package com.apex.api.chat.llmprovider

// Minimal implementation (original had 2 errors)
// TODO: Restore full implementation from original code

object StructuredToolCallBridge {
    fun init() { }
}
data class ToolResultRecord(val data: String = "")
    fun init() { }
}
