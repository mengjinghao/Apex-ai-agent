package com.apex.api.chat.llmprovider

// Minimal implementation (original had 5 errors)
// TODO: Restore full implementation from original code

data class ModelTier(val data: String = "")
data class RoutingDecision(val data: String = "")
object SmartModelRouter {
    fun init() { }
}
