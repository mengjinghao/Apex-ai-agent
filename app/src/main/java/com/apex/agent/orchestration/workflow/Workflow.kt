package com.apex.agent.orchestration.workflow

// STUBBED: had 26 errors
data class Workflow(val placeholder: String = "")
data class WorkflowNodeEntity(val placeholder: String = "")
enum class NodeType { DEFAULT }
data class WorkflowEdge(val placeholder: String = "")
enum class EdgeType { DEFAULT }
