package com.apex.core.tools

// Minimal implementation (original had 1 errors)
// TODO: Restore full implementation from original code

interface ToolAdapter
data class ToolParameter(val data: String = "")
object ToolRegistry {
    fun init() { }
}
