package com.apex.core.extension

// Minimal implementation (original had 21 errors)
// TODO: Restore full implementation from original code

interface without
enum class CoreImpact { DEFAULT }
data class CapabilityDeclaration(val data: String = "")
