package com.apex.core.chat

// Minimal implementation (original had 560 errors)
// TODO: Restore full implementation from original code

object AIMessageManager {
    fun init() { }
}
data class PackageUsageStat(val data: String = "")
data class PackageUsageCounter(val data: String = "")
