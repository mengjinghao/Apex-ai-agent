package com.apex.core.config

// Minimal implementation (original had 4500 errors)
// TODO: Restore full implementation from original code

object SystemToolPrompts {
    fun init() { }
}
    fun init() { }
}
data class ManageableToolPrompt(val data: String = "")
