package com.apex.agent.core.storage

// Minimal implementation (original had 10 errors)
// TODO: Restore full implementation from original code

class BatchRunStorage
enum class BatchStatus { DEFAULT }
data class RLStats(val data: String = "")
