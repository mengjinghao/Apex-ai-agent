package com.apex.agent.core.tools.system

// STUBBED: had 44 errors
enum class PermissionMode { DEFAULT }
data class PermissionModeState(val placeholder: String = "")
enum class RootExecutionMode { DEFAULT }
data class RootDetectionResult(val placeholder: String = "")
enum class RootScheme { DEFAULT }
enum class SELinuxStatus { DEFAULT }
data class ShizukuDetectionResult(val placeholder: String = "")
