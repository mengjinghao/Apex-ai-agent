package com.apex.agent.core.normal.festival

// Minimal implementation (original had 1 errors)
// TODO: Restore full implementation from original code

enum class FestivalType { DEFAULT }
data class Festival(val data: String = "")
data class SolarTerm(val data: String = "")
data class FestivalContext(val data: String = "")
class FestivalAwarenessSystem
