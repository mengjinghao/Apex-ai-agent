package com.apex.agent.core.performance

// Minimal implementation (original had 9 errors)
// TODO: Restore full implementation from original code

data class GlobalConfig(val data: String = "")
data class GlobalMetrics(val data: String = "")
class BufferManager
data class BufferConfig(val data: String = "")
class PerformanceTuner
data class TuningParams(val data: String = "")
