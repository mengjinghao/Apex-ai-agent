package com.apex.agent.core.normal.branching

// Minimal implementation (original had 1 errors)
// TODO: Restore full implementation from original code

data class BranchMessage(val data: String = "")
enum class Role { DEFAULT }
data class ConversationBranch(val data: String = "")
data class BranchTree(val data: String = "")
class ConversationBranching
data class BranchOption(val data: String = "")
