package com.apex.agent.core.tools.defaultTool.admin

import android.content.Context
import com.apex.agent.core.tools.defaultTool.debugger.DebuggerUITools

/** 管理员级别的UI工具，继承调试版�?/
open class AdminUITools(context: Context) : DebuggerUITools(context) {
    // 当前阶段不添加新功能，仅继承调试实现
}
