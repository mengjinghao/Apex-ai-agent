package com.apex.agent.core.emotion

// Minimal implementation (original had 244 errors)
// TODO: Restore full implementation from original code

class StyleAdapter
enum class ConversationStyle { DEFAULT }
data class StyleConfiguration(val data: String = "")
data class UserStylePreferences(val data: String = "")
