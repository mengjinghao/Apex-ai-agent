package com.apex.core.tools

// Minimal implementation (original had 33 errors)
// TODO: Restore full implementation from original code

object ToolHelper {
    fun init() { }
}
data class ToolContext(val data: String = "")
interface ExecutionProgressCallback
