package com.apex.agent.core.normal.profile

// STUBBED: had 2 errors
data class UserProfile(val placeholder: String = "")
enum class Formality { DEFAULT }
enum class Tone { DEFAULT }
enum class Verbosity { DEFAULT }
data class TechStack(val placeholder: String = "")
data class ResponsePreference(val placeholder: String = "")
enum class CodeStyle { DEFAULT }
