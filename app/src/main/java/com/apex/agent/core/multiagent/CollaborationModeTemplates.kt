package com.apex.agent.core.multiagent

// Minimal implementation (original had 1002 errors)
// TODO: Restore full implementation from original code

object CollaborationModeTemplates {
    fun init() { }
}
enum class CollaborationCategory { DEFAULT }
data class CollaborationTemplate(val data: String = "")
