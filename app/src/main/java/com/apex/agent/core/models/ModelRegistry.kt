package com.apex.agent.core.models

// Minimal implementation (original had 5 errors)
// TODO: Restore full implementation from original code

object ModelRegistry {
    fun init() { }
}
enum class QuantizationFormat { DEFAULT }
data class LocalModelInfo(val data: String = "")
