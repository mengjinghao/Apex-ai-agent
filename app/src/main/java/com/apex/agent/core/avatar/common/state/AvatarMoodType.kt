package com.apex.agent.core.avatar.common.state

// Minimal implementation (original had 30 errors)
// TODO: Restore full implementation from original code

data class AvatarCustomMoodDefinition(val data: String = "")
data class AvatarMoodTypeDefinition(val data: String = "")
object AvatarMoodTypes {
    fun init() { }
}
