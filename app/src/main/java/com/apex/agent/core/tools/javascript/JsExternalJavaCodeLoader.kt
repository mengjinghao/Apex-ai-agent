package com.apex.core.tools.javascript

// Minimal implementation (original had 1 errors)
// TODO: Restore full implementation from original code

class PrefixIsolatedDexClassLoader
class JsExternalJavaCodeLoader
enum class SourceType { DEFAULT }
data class LoadOptions(val data: String = "")
data class LoadedArtifact(val data: String = "")
