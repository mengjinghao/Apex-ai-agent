package com.apex.agent.core.mcp

// Minimal implementation (original had 24 errors)
// TODO: Restore full implementation from original code

class MCPServerBridge
class ClientHandler
data class MCPRequest(val data: String = "")
data class MCPResponse(val data: String = "")
    fun init() { }
}
data class MCPEntry(val data: String = "")
