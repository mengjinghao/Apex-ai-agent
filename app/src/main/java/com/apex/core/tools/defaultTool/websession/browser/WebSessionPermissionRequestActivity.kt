package com.apex.core.tools.defaultTool.websession.browser

import android.app.Activity
import android.os.Bundle

/** WebSessionPermissionRequestActivity — 透明占位 Activity。 */
class WebSessionPermissionRequestActivity : Activity() {
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        finish()
    }
}
