package com.apex.agent.plugins.burst.builtin

class KnowledgeGraphSkill

enum class EntityType { DEFAULT }

enum class RelationType { DEFAULT }

data class GraphEntity(val placeholder: String = "")

data class GraphRelation(val placeholder: String = "")

data class GraphStats(val placeholder: String = "")
